"""Save audio data to a file."""
# # save_audio_data.py
# data = hass.states.get("sensor.esphome_encoded_audio").state

# # notify サービスを使ってエンコードされたデータをファイルに保存
# hass.services.call("notify", "save_audio_data", {"message": data})
